# Criticas para recargar MODEL/artifacts/model.pkl y reproducir metricas:
# rango acotado (evita drift silencioso entre majors sin sobre-restringir).
numpy>=1.26,<3
pandas>=2.2,<3
# Los dos modelos protegidos se serializaron con 1.8.0. Cambiar esta version
# requiere validar/migrar artefactos por la puerta; un upgrade de pip no basta.
scikit-learn==1.8.0
scipy>=1.13
matplotlib>=3.8
lightgbm>=4.5,<5
xgboost>=2.1
shap>=0.46
# Numba 0.66.0 es rechazado por Windows Application Control en CPython 3.13;
# 0.65.1 tiene wheel cp313 y supera import, JIT y TreeExplainer en staging.
numba>=0.65.1,<0.66
optuna>=4.5
PyYAML>=6.0.2
# El trainer puede omitir candidatos CatBoost por flag, pero el entorno bloqueado
# lo incluye para que el conjunto completo de algoritmos sea reproducible.
catboost>=1.2 ; python_version < "3.14"

requests>=2.32.4,<3
parsel>=1.10
packaging>=24,<27

pytest>=8.0.0
pytest-cov>=4.0.0
ruff>=0.12.0
mypy>=1.16.0
